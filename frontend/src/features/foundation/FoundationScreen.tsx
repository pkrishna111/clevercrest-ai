const tokenGroups = [['Canvas', 'Mineral foundation with quiet tonal texture.'], ['Surfaces', 'Bone panels, Mist borders, and restrained elevation.'], ['Accent', 'Deep Moss actions and crest-inspired active states.']]
export function FoundationScreen() { return <section className="foundation-screen" id="foundation" aria-labelledby="foundation-title"><div className="crest-divider" aria-hidden="true"><span /><span /></div><p className="eyebrow">CleverCrest AI</p><h1 id="foundation-title">A calm foundation for intelligent knowledge.</h1><p className="foundation-intro">The shared visual language is ready for future product modules, without introducing product features.</p><div className="foundation-grid" id="surfaces">{tokenGroups.map(([title, description], index) => <article className="foundation-card" key={title}><span className="card-index">0{index + 1}</span><h2>{title}</h2><p>{description}</p></article>)}</div><section className="theme-note" id="themes" aria-labelledby="theme-note-title"><div><p className="eyebrow">Theme system</p><h2 id="theme-note-title">Quiet by default, deliberate in either light.</h2></div><p>Color, typography, spacing, radius, borders, and focus treatment are centralized as reusable tokens.</p></section></section> }
